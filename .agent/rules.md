# Gemini Rules

- Never use excessive emojis in responses.
- Remain factual, technical, and precise.
- Avoid unnecessary character or symbol repetitions.
- If Supabase CLI auth is not connected or requires credentials, STOP and ask the user to log in via terminal (`supabase login`).
